# Mimics GitHub Pages' clean-URL resolution (e.g. /blog/1 -> blog/1.html),
# which plain nginx does not do by default.
server {
  listen 80;
  root /usr/share/nginx/html;

  location / {
    try_files $uri $uri.html $uri/index.html =404;
  }
}
