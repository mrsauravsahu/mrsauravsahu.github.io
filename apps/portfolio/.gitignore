.DS_Store
node_modules
/.svelte-kit
/package
/static/data/blog-posts
/static/photos-opt

# old .gitignore

/cypress/screenshots

/build
!/BUILD
.env
build
# Timestamped output dirs from build-site.sh. Digit-anchored so the script
# itself isn't swallowed by its own ignore rule.
build-[0-9]*
